h1 = @note.title
p 建立日期：#{@note.created_at.to_date}
p 上次修改日期：#{@note.updated_at.to_date}
h3 內容
/ .col-lg-12
.content-panel
  .content-panel-text = sanitize_content(@note.content)

h3 反思回饋筆記
/ .col-lg-12
.content-panel
  .content-panel-text = sanitize_content(@note.final_content)



= link_to '編輯', edit_note_path(@note)
'|
= link_to '返回', notes_path
